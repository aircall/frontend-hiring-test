import { REQUEST_PROVIDER_TYPE } from '@core/models/request-provider.enum';

export const requestProviderType: REQUEST_PROVIDER_TYPE[] = [
  REQUEST_PROVIDER_TYPE.GraphQL,
  REQUEST_PROVIDER_TYPE.RestAPI,
];
